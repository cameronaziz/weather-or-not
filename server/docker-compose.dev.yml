version: '3.8'

services:
  weather-or-not-server:
    build:
      context: ..
      dockerfile: server/Dockerfile
      target: development
    ports:
      - "9876:9876"
    volumes:
      - ..:/app
      - /app/node_modules
      - /app/server/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - ../.env
    restart: unless-stopped